import React from 'react'
import { RouteComponentProps } from 'react-router-dom'

const PlayList = (props: RouteComponentProps) => {
  console.log(props)
  return <div>playlist</div>
}
export default PlayList
